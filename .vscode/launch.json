{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Quant Desk KR (Streamlit)",
            "type": "debugpy",
            "request": "launch",
            "module": "streamlit",
            "args": ["run", "${workspaceFolder}/app.py", "--server.address", "127.0.0.1"],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "justMyCode": true
        }
    ]
}
